<%= form_for Invite.new, :url => invites_path, method: :post do |f| %>
    <%= f.hidden_field :pantry_id, :value => @pantry.id %>
    <%= f.label :Invite %>
    <%= f.email_field :email, :placeholder => "email" %>
    <%= f.submit 'Send' %>
<% end %>
